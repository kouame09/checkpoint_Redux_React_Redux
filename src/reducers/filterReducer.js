const initialState = {
    tasks: [], // Initialisez tasks comme un tableau vide
  };
  
  const taskReducer = (state = initialState, action) => {
    // Votre logique de reducer pour les actions ADD_TASK, TOGGLE_TASK, DELETE_TASK
  };
  